<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>TODO LIST</title>
        {% block stylesheets %}
            {{ encore_entry_link_tags('app') }}
        {% endblock %}
        {% block javascripts %}
            {{ encore_entry_script_tags('app') }}
        {% endblock %}
    </head>
    <body>
        {% block body %}
            {{ component('todo_list', {
                'todoList': todo_list,
                'form': form
            }) }}
        {% endblock %}
    </body>
</html>
